<Page x:Class="Great2.Views.Pages.ImportExportView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Great2.Views.Pages"
      mc:Ignorable="d" 
      DataContext="{Binding ImportExport, Source={StaticResource Locator}}"
      Title="ImportExportView"
      Style="{DynamicResource MahApps.Styles.Page}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <Button Content="Import GREAT" Width="100" Height="100" Click="GreatImportButton_Click"/>
            <Separator/>
            <Button Content="Import FDL" Width="100" Height="100" Click="FDLImportButton_Click"/>
        </StackPanel>        
    </Grid>
</Page>
