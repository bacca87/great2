<Page x:Class="Great.Views.Pages.CarRental"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      xmlns:local="clr-namespace:Great.Views.Pages"
      xmlns:m="clr-namespace:Great.Models.Database"
      xmlns:conv="clr-namespace:Great.Utils.Converters"
      xmlns:toolkit="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Input.Toolkit"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      DataContext="{Binding CarRental, Source={StaticResource Locator}}"
      Title="CarRental"
       Style="{DynamicResource MetroPage}">

    <Page.Resources>
        <CollectionViewSource x:Key="CarRentalCollectionView" 
                              Source="{Binding Rentals}" IsLiveSortingRequested="True" >
        </CollectionViewSource>

        <DataTemplate x:Key="AutoCompleteLicensePlate">
            <StackPanel Orientation="Horizontal">
                <Label
            Content="{Binding LicensePlate}"
            Width="100" />
                <Label
            Content="{Binding Model}"
            FontStyle="Italic"
            Foreground="DarkGray" />
            </StackPanel>
        </DataTemplate>

    </Page.Resources>

    <Grid Margin="10,0,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Margin="10,0,10,0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" >
                <TextBlock>Rent Start Date:</TextBlock>
                <DatePicker SelectedDate=""></DatePicker>
                <TextBlock>Rent End Date</TextBlock>
                <DatePicker SelectedDate=""></DatePicker>
                <TextBlock>Car Plate</TextBlock>
                <TextBox Width="100" Height="20"></TextBox>
                <Button Margin="10" Content="Apply Filter" Command="{Binding ApplyFilter}" />
                <Button Margin="10" Content="Clear Filter" Command="{Binding ClearFilter}"  />
            </StackPanel>

            <DataGrid Name="CarRentalHistoryView"
                Grid.Row="1"
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"
                AutoGenerateColumns="False" 
                CanUserReorderColumns="False" 
                CanUserResizeColumns="False" 
                CanUserResizeRows="False" 
                CanUserSortColumns="False" 
                IsReadOnly="True"                  
                SelectionMode="Single"
                HeadersVisibility="Column"
                ItemsSource="{Binding Source={StaticResource CarRentalCollectionView}}"
                SelectedItem="{Binding SelectedRent}"
                VirtualizingPanel.ScrollUnit ="Item" 
                VirtualizingPanel.VirtualizationMode="Standard">
                <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Style.Setters>
                            <Setter Property="TextBlock.TextAlignment" Value="Left" />
                            <Setter Property="TextBlock.FontSize" Value="15" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        </Style.Setters>
                    </Style>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="{x:Static SystemColors.HighlightColor}"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{x:Static SystemColors.HighlightTextColor}"/>

                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Status">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Rent Start Date" Binding="{Binding RentStartDate,StringFormat='dd/MM/yyyy' }" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Rent End Date" Binding="{Binding RentEndDate,StringFormat='dd/MM/yyyy'}"/>
                    <DataGridTextColumn Header="Car Brand" Binding="{Binding Car1.Brand}"/>
                    <DataGridTextColumn Header="Car Model" Binding="{Binding Car1.Model}"/>
                    <DataGridTextColumn Header="Car Brand" Binding="{Binding Car1.LicensePlate}"/>
                </DataGrid.Columns>
            </DataGrid>


        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Vertical">
                <TextBlock>Car Plate</TextBlock>
                <toolkit:AutoCompleteBox 
                    ItemsSource="{Binding Cars }" 
                    ValueMemberPath="LicensePlate"  
                    SelectedItem="{Binding SelectedCarClone}"
                    ItemTemplate="{StaticResource AutoCompleteLicensePlate}"/>
                <!--="{Binding SelectedCarClone.LicensePlate}" IsEnabled="{Binding IsInputEnabled}"/>-->
                <TextBlock>Brand</TextBlock>
                <TextBox Text="{Binding SelectedCarClone.Brand}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Model</TextBlock>
                <TextBox Text="{Binding SelectedCarClone.Model}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent Company</TextBlock>
                <ComboBox 
                                  SelectedItem="{Binding SelectedCarClone.CarRentalCompany1}" 
                                  SelectedValue="{Binding SelectedCarClone.CarRentalCompany}" 
                                  IsEnabled="{Binding IsInputEnabled}" 
                                  DisplayMemberPath="Name" 
                                  SelectedValuePath="Id" 
                                  SelectedIndex="0">
                    <ComboBox.Resources>
                        <CollectionViewSource x:Key="Items" Source="{Binding RentalCompanies}" />
                    </ComboBox.Resources>
                    <ComboBox.ItemsSource>
                        <CompositeCollection>
                            <m:CarRentalCompany/>
                            <CollectionContainer Collection="{Binding Source={StaticResource Items}}" />
                        </CompositeCollection>
                    </ComboBox.ItemsSource>
                </ComboBox>
                <TextBlock>Initial Km</TextBlock>
                <TextBox
                    Text="{Binding SelectedRentClone.StartKm}" 
                    IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Ending Km</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.EndKm}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent Start Date:</TextBlock>
                <DatePicker DisplayDate="{Binding SelectedRentClone.RentStartDate}" SelectedDate="{Binding SelectedRentClone.RentStartDate}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent End Date</TextBlock>
                <DatePicker  DisplayDate="{Binding SelectedRentClone.RentEndDate}" SelectedDate="{Binding SelectedRentClone.RentEndDate}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent Start Location</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.StartLocation}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent End Location</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.EndLocation}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Notes</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.Notes}" IsEnabled="{Binding IsInputEnabled}"/>
            </StackPanel>


        </Grid>
    </Grid>
</Page>
