<Page x:Class="Great.Views.Pages.CarRental"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
      xmlns:local="clr-namespace:Great.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
            DataContext="{Binding CarRental, Source={StaticResource Locator}}"
      Title="CarRental">

    <Page.Resources>

    </Page.Resources>

    <Grid Margin="10,0,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Margin="10,0,10,0" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" >
                <TextBlock>Rent Start Date:</TextBlock>
                <DatePicker SelectedDate=""></DatePicker>
                <TextBlock>Rent End Date</TextBlock>
                <DatePicker SelectedDate=""></DatePicker>
                <TextBlock>Car Plate</TextBlock>
                <TextBox Width="100" Height="20"></TextBox>
                <Button Margin="10" Content="Apply Filter" Command="{Binding ApplyFilter}" />
                <Button Margin="10" Content="Clear Filter" Command="{Binding ClearFilter}"  />
            </StackPanel>

            <DataGrid Name="CarRentalHistoryView"
                Grid.Row="1"
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"
                AutoGenerateColumns="False" 
                CanUserReorderColumns="False" 
                CanUserResizeColumns="False" 
                CanUserResizeRows="False" 
                CanUserSortColumns="False" 
                IsReadOnly="True"                  
                SelectionMode="Single"
                HeadersVisibility="Column"
                ItemsSource="{Binding Rentals}"
                SelectedItem="{Binding SelectedRent}"
                VirtualizingPanel.ScrollUnit ="Item" 
                VirtualizingPanel.VirtualizationMode="Standard">
                <DataGrid.Resources>
                    <Style TargetType="DataGridCell">
                        <Style.Setters>
                            <Setter Property="TextBlock.TextAlignment" Value="Left" />
                            <Setter Property="TextBlock.FontSize" Value="15" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                        </Style.Setters>
                    </Style>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="{x:Static SystemColors.HighlightColor}"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="{x:Static SystemColors.HighlightTextColor}"/>

                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Status">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">

                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Rent Start Date" Binding="{Binding StartDate }" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Rent End Date" Binding="{Binding EndDate}"/>
                    <DataGridTextColumn Header="Car" Binding="{Binding Car1.Brand}"/>
                </DataGrid.Columns>
            </DataGrid>


        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Vertical">
                <TextBlock>Car Plate</TextBlock>
                <TextBox  Text="{Binding SelectedRentClone.Car1.LicensePlate}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Brand</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.Car1.Brand}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Model</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.Car1.Model}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent Company</TextBlock>
                <ComboBox ItemsSource="{Binding RentalCompanies}" SelectedItem="{Binding SelectedRentClone.Car1.CarRentalCompany1}" DisplayMemberPath="Name" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Initial Km</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.StartKm}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Ending Km</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.EndKm}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Driven Km</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.EndKm}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent Start Date:</TextBlock>
                <DatePicker SelectedDate="{Binding SelectedRentClone.StartDate}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent End Date</TextBlock>
                <DatePicker SelectedDate="{Binding SelectedRentClone.EndDate}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent Start Location</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.StartLocation}" IsEnabled="{Binding IsInputEnabled}"/>
                <TextBlock>Rent End Llocation</TextBlock>
                <TextBox Text="{Binding SelectedRentClone.EndLocation}" IsEnabled="{Binding IsInputEnabled}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Margin="10" Content="Save" Command="{Binding SaveCommand}" CommandParameter="{Binding SelectedRentClone}" IsEnabled="True"  />
                <Button Margin="10" Content="Delete" Command="{Binding DeleteRentCommand}"  />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
